name: otpify
description: Otpify is a customizable OTP input field widget for Flutter, offering a responsive design that adapts to various screen sizes. It includes a resend code timer button for added convenience.
version: 0.0.2
homepage: https://github.com/mibra-heem/otpify

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
